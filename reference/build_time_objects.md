# Build time objects for interpolation

Build time objects for interpolation

## Usage

``` r
build_time_objects(periods_starts, periods_ends)
```

## Arguments

- periods_starts:

  Vector of periods starts

- periods_ends:

  Vector of periods ends

## Value

A list with the timing and the number of weeks
